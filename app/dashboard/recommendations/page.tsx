"use client";
import {useEffect,useState} from "react";
export default function Recommendations(){const [d,setD]=useState<any>(null);useEffect(()=>{fetch("/api/dashboard/recommendations").then(r=>r.json()).then(setD)},[]);if(!d)return <div className="card"><h2>Формирую рекомендации…</h2></div>;return <div><div className="pageHead"><div><p className="eyebrow">AI-РЕКОМЕНДАЦИИ</p><h1>Что делать руководителю</h1><p className="muted">Приоритеты обучения команды на основе результатов тренировок.</p></div></div><div className="recommendList">{d.recommendations.map((x:any)=><div className={"card recRow "+x.priority} key={x.employeeId}><div><b>Менеджер {x.employeeId.slice(0,8)}…</b><small>Средний результат: {x.score}% · слабый навык: {x.focus}</small></div><div><strong>{x.action}</strong><span>{x.priority==="high"?"Высокий приоритет":x.priority==="medium"?"Средний приоритет":"Низкий приоритет"}</span></div></div>)}</div></div>}
