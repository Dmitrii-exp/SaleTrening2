import {NextResponse} from "next/server"; import {createClient} from "@/lib/supabase/server";
export async function GET(){const s=await createClient();const {data:c}=await s.auth.getClaims();if(!c?.claims?.sub)return NextResponse.json({error:"Unauthorized"},{status:401});const {data:p}=await s.from("profiles").select("company_id,role").eq("id",c.claims.sub).maybeSingle();if(!p?.company_id)return NextResponse.json({error:"Company not found"},{status:403});const {data:rows}=await s.from("employee_learning_profiles").select("employee_id,total_sessions,average_score,current_difficulty").eq("company_id",p.company_id).order("average_score",{ascending:false});const {data:team}=await s.from("profiles").select("id,first_name,last_name").eq("company_id",p.company_id);const names=new Map((team||[]).map(x=>[x.id,[x.first_name,x.last_name].filter(Boolean).join(" ")]));const leaderboard=(rows||[]).map((x:any,i:number)=>({rank:i+1,employeeId:x.employee_id,name:names.get(x.employee_id)||"Менеджер",sessions:x.total_sessions,score:x.average_score,level:x.current_difficulty}));return NextResponse.json({leaderboard})}
