import {NextResponse} from "next/server"; import {createClient} from "@/lib/supabase/server";
export async function GET(req:Request){const s=await createClient();const {data:c}=await s.auth.getClaims();if(!c?.claims?.sub)return NextResponse.json({error:"Unauthorized"},{status:401});const id=new URL(req.url).searchParams.get("sessionId");if(!id)return NextResponse.json({error:"sessionId required"},{status:400});const {data:p}=await s.from("profiles").select("company_id").eq("id",c.claims.sub).maybeSingle();if(!p?.company_id)return NextResponse.json({error:"Company not found"},{status:403});const {data,ferror}=await s.from("realtime_training_sessions").select("id,scenario_id,score,engine_state,created_at,duration_seconds").eq("id",id).eq("company_id",p.company_id).maybeSingle();if(ferror)return NextResponse.json({error:ferror.message},{status:400});if(!data)return NextResponse.json({error:"Session not found"},{status:404});const {data:feedback}=await s.from("realtime_training_feedback").select("*").eq("session_id",id).maybeSingle();return NextResponse.json({session:data,feedback})}
