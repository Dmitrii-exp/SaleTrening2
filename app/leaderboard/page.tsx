"use client";
import {useEffect,useState} from "react";
export default function Leaderboard(){const [d,setD]=useState<any>(null);useEffect(()=>{fetch("/api/leaderboard").then(r=>r.json()).then(setD)},[]);if(!d)return <div className="card"><h2>Загрузка рейтинга…</h2></div>;return <div><div className="pageHead"><div><p className="eyebrow">КОМАНДА</p><h1>Рейтинг обучения</h1><p className="muted">Рейтинг строится по реальным результатам AI-тренировок.</p></div></div><div className="leaderboard">{d.leaderboard.map((x:any)=><div className={"card rankRow "+(x.rank<=3?"top":"")} key={x.employeeId}><div className="rank">#{x.rank}</div><div><b>{x.name}</b><small>{x.sessions} тренировок · уровень {x.level}</small></div><strong>{x.score}%</strong></div>)}</div>{!d.leaderboard.length&&<div className="card"><h2>Пока нет рейтинга</h2></div>}</div>}
