# Stage 46 — Training Notifications

Adds an in-app notification center:
- training reminders
- goal reminders
- unread/read state
- employee/company scoped RLS

This stage deliberately does not invent a delivery provider. Email/push/Telegram can be connected later without exposing secrets to the client.
